let operationManager = (() => {
    let addOperation = (op) => {
        let operation = {
            type: op.type
        };
        if (op.type === "operation") {
            operation.process = "";
        }
    };
});
export {};
//# sourceMappingURL=buttonListener.js.map